apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment
  labels:
    app: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: nanajanashia-demo-app:v1.o
        image: nginx:8.0
        ports:
        - containerPort: 3000
      
        -name:USER-NAME
        value from:
        secretekey ref:
        name: mongo-secret
        key: mongo-USER
        -name:USER-pwd       
        value from:
        secretekey ref:
        name: mongo-secret
        key: mongo-password
-name:DB_URL
valuefrom:
configmapkeyref:

name:mongo-confg
key:mongo-url
      
        ---
          apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
type: NodePort
  selector:
    app.kubernetes.io/name: webapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort:30010
